{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 当前文件",
            "console": "integratedTerminal",
            "cwd": "${workspaceRoot}",
            "program": "${file}",
            "request": "launch",
            "type": "python",
            "justMyCode": true
        }
    ]
}
